# #!/bin/sh
#     cd ~/backend	
#     sudo git pull origin main
#     curl -o-   https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh    | bash
#     . ~/.nvm/nvm.sh 
#     pm2 restart app.js
#     exit
ssh ubuntu@3.38.246.250 <<EOF
    cd ~/backend		
    sudo git pull origin main
    curl -o-   https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh    | bash
    . ~/.nvm/nvm.sh 
    pm2 restart app.js
    exit
EOF

